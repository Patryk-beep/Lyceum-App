# Don't auto-run `install` before every `pnpm run` (it fails on the esbuild
# build-script approval prompt; esbuild already resolves its native binary from
# its platform optional-dependency). A fresh `pnpm install` honours the
# `pnpm.onlyBuiltDependencies` allowlist in package.json.
verify-deps-before-run=false
