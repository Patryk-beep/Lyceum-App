# OS / sync cruft
.DS_Store
Thumbs.db
desktop.ini
*~

# Editor / tooling
.vscode/
.idea/

# Node (for when the app build lands here)
node_modules/
dist/
build/
.env
.env.*
*.log

# Rust build output
target/

# Working / process docs — useful locally, but not crucial to the app or to the
# GitHub repo's functionality. Kept on disk, out of version control.
.rune/
/PLAN.md
/QUESTIONS-FOR-REVIEW.md
/SESSION-HANDOFF.md

# Tooling cruft (claude-flow / ruflo session + data artifacts)
.claude-flow/
.swarm/
.hive-mind/
ruvector.db
ruvector.db-shm
ruvector.db-wal
config/
.claude/
.context/
/.github/copilot-instructions.md
# Root-level tooling cruft (NOT the app's app/package.json — anchored to repo root)
/package.json
/package-lock.json
/CLAUDE.md
/skills-lock.json
# pnpm-9 CI landmine: a packages-less workspace file aborts every CI leg (regenerated by tooling)
app/pnpm-workspace.yaml
