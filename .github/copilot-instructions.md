

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v7.25.2 -->
<!-- Updated: 2026-06-22T20:30:15.471Z -->
<!-- sigmap: version=7.25.2 -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures
## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| To rank files by topic | `sigmap --query "<topic>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

Always run `sigmap ask` (or `sigmap --query`) before searching for files relevant to a task.

## deps
```
app/src/components/RichMarkdown.tsx ← LessonImg, MermaidDiagram
app/src/components/SessionConsole.tsx ← stores/useEngineStore
app/src/components/SessionDrawer.tsx ← stores/useEngineStore, SessionConsole
app/src/components/SkillRunOverlay.tsx ← lib/types, stores/useEngineStore, SkillRunProgress
app/src/components/SkillRunProgress.tsx ← hooks/useElapsed, lib/types, stores/useEngineStore, SessionConsole
app/src/components/SubjectCard.tsx ← theme/stages, lib/types, MasteryRing, StageChip
app/src/components/SubmissionEditor.tsx ← lib/types, RichMarkdown
app/src/components/ZenEditor.tsx ← lib/types, stores/useZenStore, SubmissionEditor
app/src/lib/placement.ts ← types
app/src/lib/query.ts ← hooks/useResumeState, ipc
app/src/lib/useSession.ts ← stores/useEngineStore, engine
app/src/routes/Analytics.tsx ← components/MasteryHeatmap, components/MasteryMeter, components/StatGrid, lib/query, lib/types
app/src/routes/Assignments.tsx ← components/ArtifactView, components/ConfirmDestructive, components/ZenEditor, lib/ipc, lib/query
app/src/routes/Capstone.tsx ← components/ArtifactView, components/MasterySeal, components/ZenEditor, lib/ipc, lib/query
app/src/routes/Dashboard.tsx ← components/ConfirmDestructive, hooks/useResumeState, components/ResumeHero, components/ReviewDueCard, components/SectionDivider
app/src/routes/Diagnostics.tsx ← lib/engine, lib/types, stores/useEngineStore
app/src/routes/Lessons.tsx ← components/ConfirmDestructive, lib/query, lib/types
app/src/routes/Placement.tsx ← components/PlacementFeedback, components/RichMarkdown, components/ZenEditor, lib/ipc, lib/placement
app/src/routes/Reader.tsx ← components/ArtifactView, components/ConfirmDestructive, lib/query
app/src/routes/Review.tsx ← components/ReviewCard, lib/ipc, lib/query, lib/types
app/src/routes/Roadmap.tsx ← components/ConfirmDestructive, components/MasterySeal, components/MasteryMeter, lib/ipc, lib/query
app/src/routes/SubjectWizard.tsx ← components/CreationProgress, lib/ipc, lib/slug, stores/useEngineStore
app/src/stores/useEngineStore.ts ← lib/types
app/src/theme/loop.ts ← lib/types
```

## changes (last 10 commits — 34 hours ago)
```
app/src/components/RichMarkdown.tsx           +RichMarkdown
app/src/components/SessionConsole.tsx         +SessionConsole  ~SessionConsole
app/src/components/SessionDrawer.tsx          +SessionDrawer  ~SessionDrawer
app/src/components/SkillRunOverlay.tsx        +SkillRunOverlay
app/src/components/SkillRunProgress.tsx       +skillLabel  +SkillRunProgress
app/src/components/SubjectCard.tsx            ~SubjectCard
app/src/components/SubmissionEditor.tsx       +SubmissionEditor  +applyWrap  +applyLine
app/src/components/ZenEditor.tsx              +loadDraft  +ZenEditor
app/src/components/components.css             +remains
app/src/hooks/useElapsed.ts                   +useElapsed
app/src/hooks/useResumeState.ts               +read  +write  +slugOf  +recordRoute
app/src/lib/placement.ts                      +parsePlacementState
app/src/lib/query.ts                          +useLessons  +useDeleteSubject  +useDeleteLesson  +useDeleteAssignment
app/src/lib/slug.ts                           +slugify
app/src/lib/useSession.ts                     ~useSessionSubscription
app/src/routes/Analytics.tsx                  ~AnalyticsView  ~Analytics
app/src/routes/Assignments.tsx                +AssignmentsView  +Assignments  +AssignmentDetail
app/src/routes/Capstone.tsx                   ~Capstone
app/src/routes/Dashboard.tsx                  ~DashboardView  ~Dashboard  ~buildStats
app/src/routes/Diagnostics.tsx                ~Diagnostics  ~Check  ~runSmoke
app/src/routes/Lessons.tsx                    +LessonsView  +Lessons
app/src/routes/Placement.tsx                  +verdictMark  ~answer  ~Placement
app/src/routes/Reader.tsx                     +Artifact  ~Research  ~Lesson
app/src/routes/Review.tsx                     ~ReviewView  ~Review
app/src/routes/Roadmap.tsx                    ~RoadmapView  ~Roadmap
app/src/routes/SubjectWizard.tsx              ~SubjectWizard
app/src/stores/useEngineStore.ts              +reduceRun  +useRun  +useIsBusy  +useAnyRunning
app/src/theme/loop.ts                         +placementTaken  +certified  +currentLoopKey  +isDone
app/src-tauri/src/delete.rs                   +here
.rune/plan-aurelia-fidelity-phase1.md         ~Sigil  ~SectionDivider
.rune/plan-aurelia-fidelity-phase3.md         ~StreakCard
.rune/plan-cross-platform-delivery-phase4.md  ~checkForUpdate
```

## .github

### .github/workflows/release.yml
```
keys: [name, on, permissions, concurrency, jobs]
job: create-release
job: build
```

## .rune

### .rune/plan-aurelia-fidelity-phase1.md
```
h1 Phase 1 — Visual atoms (Sigil, SectionDivider, gilt tokens)
h2 Code contracts
h2 Tasks
h2 Rejection criteria (DO NOT)
h2 Acceptance gates
h2 Tests
h2 Cross-phase
code-fence ts
code-fence plain
```

### .rune/plan-aurelia-fidelity-phase2.md
```
h1 Phase 2 — Mastery visuals (medallion seal + opt-in gilt dial)
h2 HARD CONSTRAINTS (from red-team — non-negotiable)
h2 Tasks
h2 Failure scenarios
h2 Rejection criteria
h2 Acceptance gates
h2 Tests
h2 Cross-phase
```

### .rune/plan-aurelia-fidelity-phase3.md
```
h1 Phase 3 — Study streak (pure helper + command + card)
h2 Data flow
h2 Code contracts
h2 Algorithm (pin exactly)
h2 Tasks
h2 Failure scenarios
h2 Rejection criteria
h2 Acceptance gates
h2 Tests (Rust)
h2 Cross-phase
code-fence plain
code-fence rust
code-fence ts
```

### .rune/plan-aurelia-fidelity-phase4.md
```
h1 Phase 4 — Integration (hero dial, cover, wiring)
h2 HARD CONSTRAINTS (from red-team)
h2 Tasks
h2 Failure scenarios
h2 Rejection criteria
h2 Acceptance gates
h2 Tests
h2 Cross-phase
```

### .rune/plan-aurelia-fidelity.md
```
h1 Plan — Aurelia Visual Fidelity (hardened)
h2 Phases
h2 Key decisions (hardened)
h2 Decision compliance (project-locked)
h2 Red-team (folded from 2 adversarial series)
h2 Risks
h2 Status
```

### .rune/plan-cross-platform-delivery-phase1.md
```
h1 Phase 1 — Windows Bridge Core
h2 Research facts this phase encodes (all confidence: high)
h2 Data Flow
h2 Code Contracts
h2 Tasks
h2 Failure Scenarios
h2 Rejection Criteria (DO NOT)
h2 Cross-Phase Context
h2 Acceptance Criteria
h2 Test tasks
code-fence plain
code-fence rust
```

### .rune/plan-cross-platform-delivery-phase2.md
```
h1 Phase 2 — Windows Filesystem Hardening
h2 Research facts this phase encodes
h2 Data Flow
h2 Code Contracts
h2 Tasks
h2 Failure Scenarios
h2 Rejection Criteria (DO NOT)
h2 Cross-Phase Context
h2 Acceptance Criteria
h2 Test tasks
code-fence plain
code-fence rust
```

### .rune/plan-cross-platform-delivery-phase3.md
```
h1 Phase 3 — One-Line Installers + Ad-hoc macOS Signing
h2 Research facts this phase encodes
h2 Data Flow
h2 Code Contracts (defensive)
h1 site/install.sh (curl -fsSL https://patryk-beep.github.io/Lyceum-App/install.sh | sh)
h1 ⚑ ALWAYS send a User-Agent or GitHub returns 403; ⚑ capture grep under set -e with ||true then check empty:
h1 site/install.ps1 (irm https://patryk-beep.github.io/Lyceum-App/install.ps1 | iex)
h2 Tasks
h2 Failure Scenarios
h2 Rejection Criteria (DO NOT)
h2 Cross-Phase Context
h2 Acceptance Criteria
h2 Test tasks
code-fence plain
code-fence sh
code-fence powershell
```

### .rune/plan-cross-platform-delivery-phase4.md
```
h1 Phase 4 — Updater Fold-in + Signing Hooks (no-op until secrets)
h2 Research facts this phase encodes
h2 Data Flow
h2 Code Contracts
h2 Tasks
h2 Failure Scenarios
h2 Rejection Criteria (DO NOT)
h2 Cross-Phase Context
h2 Acceptance Criteria
h2 Test tasks
code-fence plain
code-fence jsonc
code-fence json
code-fence ts
```

### .rune/plan-cross-platform-delivery.md
```
h1 Plan — Cross-Platform Delivery (Windows bridge + one-line installers + updater)
h2 Goal
h2 Phases
h2 Key Decisions (locked with the user 2026-06-18)
h2 Decision Compliance (existing locks in PLAN.md / QUESTIONS-FOR-REVIEW.md)
h2 Architecture
h2 Dependencies / Risks (red-team-hardened 2026-06-18 — 5 lenses, confirmed findings folded into phases)
h2 Outcome Block
```

## app

### app/src/components/RichMarkdown.tsx
```
component RichMarkdown
hook useMemo
export RichMarkdown
```

### app/src/components/SessionConsole.tsx
```
component SessionConsole
hook useRun
export SessionConsole
```

### app/src/components/SessionDrawer.tsx
```
component SessionDrawer
hook useState
hook useAnyRunning
hook useRun
hook useEffect
export SessionDrawer
```

### app/src/components/SkillRunOverlay.tsx
```
component SkillRunOverlay
hook useIsBusy
hook useRef
hook useEffect
export SkillRunOverlay
```

### app/src/components/SkillRunProgress.tsx
```
component SkillRunProgress
hook useRun
hook useElapsed
export SkillRunProgress
```

### app/src/components/SubjectCard.tsx
```
component SubjectCard
export SubjectCard
handler onClick
```

### app/src/components/SubmissionEditor.tsx
```
component SubmissionEditor
hook useState
hook useRef
export SubmissionEditor
handler onStart
handler onEnd
handler onChange
```

### app/src/components/ZenEditor.tsx
```
component ZenEditor
hook useState
hook useZenStore
hook useRef
hook useEffect
export ZenEditor
handler onKey
handler onChange
handler onSubmit
handler onClick
```

### app/src/components/components.css
```
.mastery-ring
.mastery-ring__hole
.mastery-ring__value
.mastery-ring__disc
.mastery-ring__disc
.mastery-ring__label
.mastery-meter
.mastery-meter__fill
```

### app/src/hooks/useElapsed.ts
```
export function useElapsed(startedAt, running) → string  :5-17
```

### app/src/hooks/useResumeState.ts
```
export interface ResumeEntry  :13-17
  slug: string  :14-14
  route: string  :15-15
  ts: number  :16-16
export function recordRoute(pathname, now) → void  :44-50
export function resumeRoute(slug) → string  :53-55
export function recentSubjects() → ResumeEntry[]  :58-62
export function lastSubject() → string | null  :65-67
export function forgetSubject(slug) → void  :71-77
export function useResumeRecorder() → void  :80-85
```

### app/src/lib/placement.ts
```
export function parsePlacementState(raw) → PlacementState | null  :11-42
```

### app/src/lib/query.ts
```
export function useSubjects()  :6-11
export function useWorkspaceInfo()  :13-18
export function useSeedDemo()  :20-29
export function useManifest(slug)  :31-37
export function useReviewDue(slug)  :39-45
export function useAnalytics(slug)  :47-53
export function useStreak()  :55-61
export function useLessons(slug)  :63-69
export function useDeleteSubject()  :72-86
export function useDeleteLesson(slug)  :88-101
export function useDeleteAssignment(slug)  :103-112
export function useSubmitAssignment(slug)  :116-127
export function useResetCurriculum(slug)  :129-140
```

### app/src/lib/slug.ts
```
export function slugify(subject) → string  :5-18
```

### app/src/lib/types.ts
```
export interface SubjectSummary  :4-17
  slug: string  :5-5
  subject: string  :6-6
  level: number  :7-7
  target: number  :8-8
  status: string  :9-9
  phase: string  :10-10
  modulesTotal: number  :11-11
  modulesMastered: number  :12-12
export interface RouteDto  :19-23
  kind: string  :20-20
  why: string  :21-21
  target: string | null  :22-22
export interface GradePreview  :25-30
  again: number  :26-26
  hard: number  :27-27
  good: number  :28-28
  easy: number  :29-29
export interface ReviewCandidate  :32-39
  itemId: string  :33-33
  prompt: string  :34-34
  answer: string  :35-35
  moduleId: string | null  :36-36
  boxNum: number | null  :37-37
  preview: GradePreview  :38-38
```

### app/src/lib/useSession.ts
```
export function useSessionSubscription()  :7-30
```

### app/src/routes/Analytics.tsx
```
component AnalyticsView
component Analytics
hook useParams
hook useSubjects
hook useState
hook useAnalytics
export AnalyticsView
export Analytics
handler onChange
```

### app/src/routes/Assignments.tsx
```
component AssignmentsView
component Assignments
component AssignmentDetail
hook useParams
hook useManifest
hook useDeleteAssignment
hook useState
hook useQueryClient
hook useSubmitAssignment
hook useEngineStore
hook useMutation
export AssignmentsView
export Assignments
export AssignmentDetail
handler onDelete
handler onCurrentModule
handler onSubmit
```

### app/src/routes/Capstone.tsx
```
component Capstone
hook useParams
hook useQueryClient
hook useEngineStore
hook useManifest
hook useQuery
hook useMutation
export Capstone
handler onSubmit
```

### app/src/routes/Dashboard.tsx
```
component DashboardView
component Dashboard
hook useNavigate
hook useSubjects
hook useSeedDemo
hook useDeleteSubject
hook useState
export DashboardView
export Dashboard
handler onClick
handler onResume
handler onReview
handler onOpen
handler onDelete
handler onOpenSubject
handler onDeleteSubject
```

### app/src/routes/Diagnostics.tsx
```
component Check
component Diagnostics
hook useState
hook useEngineStore
hook useEffect
export Diagnostics
handler onClick
```

### app/src/routes/Lessons.tsx
```
component LessonsView
component Lessons
hook useParams
hook useLessons
hook useDeleteLesson
hook useState
export LessonsView
export Lessons
handler onDelete
```

### app/src/routes/Placement.tsx
```
component Placement
hook useParams
hook useQueryClient
hook useNavigate
hook useEngineStore
hook useQuery
hook useMutation
hook useEffect
export Placement
handler onSubmit
```

### app/src/routes/Reader.tsx
```
component Research
component Lesson
component Artifact
hook useParams
hook useNavigate
hook useLessons
hook useDeleteLesson
hook useState
export Research
export Lesson
export Artifact
```

### app/src/routes/Review.tsx
```
component ReviewView
component Review
hook useState
hook useParams
hook useQueryClient
hook useSubjects
hook useReviewDue
hook useMutation
export ReviewView
export Review
handler onGrade
handler onChange
```

### app/src/routes/Roadmap.tsx
```
component RoadmapView
component Roadmap
hook useParams
hook useQueryClient
hook useManifest
hook useEngineStore
hook useResetCurriculum
hook useState
hook useMutation
export RoadmapView
export Roadmap
handler onClick
```

### app/src/routes/SubjectWizard.tsx
```
component SubjectWizard
hook useNavigate
hook useQueryClient
hook useEngineStore
hook useState
hook useRun
hook useMutation
export SubjectWizard
handler onChange
```

### app/src/stores/useEngineStore.ts
```
export interface ToolStep  :12-16
  toolId: string  :13-13
  name: string  :14-14
  done: boolean  :15-15
export interface EngineInit  :18-24
  sessionId: string  :19-19
  apiKeySource: string | null  :20-20
  mcpServersEmpty: boolean  :21-21
  lyceumSkills: string[]  :22-22
  pluginOk: boolean  :23-23
export interface RunState  :27-39
  status: EngineStatus  :28-28
  text: string  :29-29
  thinking: string  :30-30
  tools: ToolStep[]  :31-31
  warnings: string[]  :32-32
  milestones: string[]  :34-34
  startedAt?: number  :36-36
  init?: EngineInit  :37-37
export type EngineStatus  :6-6
export function reduceRun(run, ev) → RunState  :60-118
export function useRun(slug) → RunState  :141-143
export function useIsBusy(slug) → boolean  :146-148
export function useAnyRunning() → string[]  :151-155
export const useEngineStore = create<EngineState>(...)  :120-120
```

### app/src/stores/useZenStore.ts
```
export const useZenStore = create<ZenState>(...)  :18-18
  active
  briefOpen
  available
  setActive
  toggle
  toggleBrief
  reset
```

### app/src/theme/loop.ts
```
export interface LoopStage  :18-26
  key: LoopKey  :19-19
  label: string  :20-20
  seg: string  :22-22
  status: LoopStatus  :23-23
  badge?: number  :25-25
export interface Crumb  :148-152
  label: string  :149-149
  to?: string  :151-151
export type LoopKey  :7-7
export type LoopStatus  :16-16
export function currentLoopKey(m) → LoopKey  :51-66
export function loopStages(m, opts,) → LoopStage[]  :89-108
export function segToLoopKey(seg) → LoopKey | null  :112-131
export function parseSubjectRoute(pathname)  :134-138
export function pageLabel(pathname, subjectName?) → string  :177-184
export function crumbsFor(pathname, subjectName?,) → Crumb[]  :188-221
```

### app/src/theme/primitives.css
```
var --topbar-h
.btn--primary
.btn--primary
.btn--outline
.btn--outline
.btn--ghost
.btn--ghost
.metric
.card
```

### app/src-tauri/Cargo.toml
```
table [package]
table [lib]
table [build-dependencies]
table [dependencies]
table [dev-dependencies]
table [features]
key name
key version
key edition
key license
key description
key tauri-plugin-updater
key serde_json
key tempfile
```

### app/src-tauri/resources/lyceum/references/ASSIGNMENTS.md
```
h1 ASSIGNMENTS.md — assignment-type catalog
h2 The single-point analytic rubric (attach to every brief)
h2 Productive-failure tasks: the mandatory two phases
h2 Calibrate difficulty (desirable difficulty + ZPD)
```

### app/src-tauri/resources/lyceum/references/GRAPHICS.md
```
h1 GRAPHICS.md — the lesson-graphics output contract
h2 Decision rule — pick the most reliable format that carries the idea
h2 File graphics — path & naming (SVG only)
h2 Guardrails (FORBID / REQUIRE)
code-fence bash
code-fence ---
```

### app/src-tauri/resources/lyceum/references/MANIFEST.md
```
h1 MANIFEST.md — the state contract
h2 The two contracts (non-negotiable)
h2 Workspace layout
h2 `manifest.json` schema
h2 Field notes that matter for correctness
h3 ID allocation (all skills follow this)
h2 `progress.md` format (write this shape every time)
h1 Progress — <Subject>
h2 Where you are
h2 Module map
h2 Recent history
code-fence plain
code-fence json
code-fence ---
code-fence markdown
```

### app/src-tauri/resources/lyceum/references/PLACEMENT.md
```
h1 PLACEMENT.md — placement-test blueprint
h2 Turn-per-exchange model
h2 Item selection
h2 Adaptive logic (rule-based — ship this for v1)
h2 Score → starting level
h2 After the test (when you set `done: true`)
code-fence plain
```

### app/src-tauri/resources/lyceum/skills/assess-understanding/SKILL.md
```
h1 assess-understanding
h2 Read first
h2 Process
h2 State writes
h2 Guardrails
h2 Machine output (for the Lyceum app)
```

### app/src-tauri/resources/lyceum/skills/capstone/SKILL.md
```
h1 capstone
h2 Read first
h2 Process
h2 State writes
h2 Guardrails
h2 Machine output (for the Lyceum app)
```

### app/src-tauri/resources/lyceum/skills/create-assignment/SKILL.md
```
h1 create-assignment
h2 Read first
h2 Process
h2 State writes
h2 Guardrails
```

### app/src-tauri/resources/lyceum/skills/learn/SKILL.md
```
h1 learn
h2 Read first
h2 Process
h3 Resolve the subject
h3 ROUTER
h3 Finish
h2 State writes
h2 Guardrails
```

### app/src-tauri/resources/lyceum/skills/placement-test/SKILL.md
```
h1 placement-test
h2 Read first
h2 The two coordination files (in `learning/<slug>/`)
h2 Choose your mode (decide it from the files)
h2 Adaptive logic (per PLACEMENT.md)
h2 Guardrails
```

### app/src-tauri/resources/lyceum/skills/research-topic/SKILL.md
```
h1 research-topic
h2 Read first
h2 Process
h2 State writes
h2 Guardrails
```

### app/src-tauri/resources/lyceum/skills/teach-lesson/SKILL.md
```
h1 teach-lesson
h2 Read first
h2 Process
h2 State writes
h2 Guardrails
h2 Machine output (for the Lyceum app)
```

### app/src-tauri/src/commands.rs
```
pub fn workspace_info(state: State<AppState>) → WorkspaceInfo
pub fn list_subjects(state: State<AppState>) → Vec<SubjectSummary>
pub fn read_manifest(state: State<AppState>, slug: String) → AppResult<Manifest>
pub fn compute_next_step(state: State<AppState>, slug: String) → AppResult<RouteDto>
pub fn regenerate_progress(state: State<AppState>, slug: String) → AppResult<String>
pub fn seed_demo(state: State<AppState>) → AppResult<String>
pub fn review_due(state: State<AppState>, slug: String) → AppResult<Vec<ReviewCandida...
pub fn review_grade(state: State<AppState>, slug: String, item_id: String, grade: String,) → AppResult<Manifest>
pub fn subject_analytics(state: State<AppState>, slug: String,) → AppResult<lyceum_core::anal...
pub fn study_streak(state: State<AppState>) → lyceum_core::streak::Streak...
pub fn read_artifact(state: State<AppState>, slug: String, relpath: String) → AppResult<String>
pub fn list_lessons(state: State<AppState>, slug: String,) → AppResult<Vec<crate::delete...
pub fn delete_lesson(state: State<AppState>, slug: String, module_id: String, file: String,) → AppResult<crate::delete::De...
pub fn delete_assignment(state: State<AppState>, slug: String, assignment_id: String,) → AppResult<Manifest>
pub fn submit_assignment(state: State<AppState>, slug: String, assignment_id: String, content: String,) → AppResult<Manifest>
pub fn submit_capstone(state: State<AppState>, slug: String, content: String,) → AppResult<Manifest>
pub fn submit_placement_answer(state: State<AppState>, slug: String, id: String, answer: String,) → AppResult<()>
pub fn placement_finalize(state: State<AppState>, slug: String, level: u8, evidence: String,) → AppResult<Manifest>
pub fn get_levels() → Vec<(u8, &'static str, &'st...
```

### app/src-tauri/src/delete.rs
```
pub struct DeleteLessonResult
pub struct LessonEntry
pub fn delete_subject_dir(ws: &Path, slug: &str) → AppResult<()>
pub fn delete_lesson(ws: &Path, slug: &str, module_id: &str, file: &str, today: Date,) → AppResult<DeleteLessonResult>
pub fn delete_assignment(ws: &Path, slug: &str, assignment_id: &str, today: Date,) → AppResult<Manifest>
pub fn reset_curriculum(ws: &Path, slug: &str, today: Date) → AppResult<Manifest>
pub fn list_lessons(ws: &Path, slug: &str) → AppResult<Vec<LessonEntry>>
```

### app/src-tauri/src/engine_cmds.rs
```
pub struct PreflightReport
pub struct StepDto
pub fn preflight(state: State<AppState>) → PreflightReport
pub async fn claude_doctor(state: State<'_, AppState>) → AppResult<DoctorReport>
pub async fn claude_smoke(app: AppHandle, state: State<'_, AppState>, prompt: String,) → AppResult<String>
pub async fn create_subject(app: AppHandle, state: State<'_, AppState>, subject: String, target: u8, start: String,) → AppResult<String>
pub async fn run_subject_step(app: AppHandle, state: State<'_, AppState>, slug: String,) → AppResult<StepDto>
pub async fn delete_subject(state: State<'_, AppState>, slug: String) → AppResult<()>
pub async fn reset_curriculum(state: State<'_, AppState>, slug: String) → AppResult<Manifest>
```

### app/src-tauri/src/lib.rs
```
pub fn run()
```

### app/src-tauri/src/prompts.rs
```
pub fn prompt_for(route: &Route, slug: &str) → Option<String>
```

### app/src-tauri/src/service.rs
```
pub struct WorkspaceInfo
pub struct RouteDto
pub struct ReviewCandidate
pub struct GradePreview
pub fn workspace_info(ws: &Path) → WorkspaceInfo
pub fn read_manifest(ws: &Path, slug: &str) → AppResult<Manifest>
pub fn summaries(ws: &Path, today: Date) → Vec<SubjectSummary>
pub fn next_step(ws: &Path, slug: &str) → AppResult<RouteDto>
pub fn regenerate_progress(ws: &Path, slug: &str, today: Date) → AppResult<String>
pub fn seed_demo(ws: &Path, today: Date) → AppResult<String>
pub fn ensure_workspace(ws: &Path) → AppResult<()>
pub fn scaffold_subject_dirs(dir: &Path) → std::io::Result<()>
pub fn slugify(subject: &str) → String
pub fn parse_grade(s: &str) → AppResult<lyceum_core::srs:...
pub fn review_due(ws: &Path, slug: &str, today: Date) → AppResult<Vec<ReviewCandida...
pub fn grade_review(ws: &Path, slug: &str, item_id: &str, grade: &str, today: Date,) → AppResult<Manifest>
pub fn subject_analytics(ws: &Path, slug: &str, today: Date,) → AppResult<lyceum_core::anal...
pub fn study_streak(ws: &Path, today: Date) → lyceum_core::streak::Streak...
pub fn read_artifact(ws: &Path, slug: &str, relpath: &str) → AppResult<String>
pub fn placement_finalize(ws: &Path, slug: &str, level: u8, evidence: String, today: Date,) → AppResult<Manifest>
```

### app/src-tauri/src/state.rs
```
pub struct AppState
impl AppState
  pub fn engine_ready(&self) → bool
  pub async fn session_cell(&self, slug: &str) → SessionCell
```

### app/src-tauri/src/submit.rs
```
pub fn submit_assignment(ws: &Path, slug: &str, assignment_id: &str, content: &str, today: Date,) → AppResult<Manifest>
pub fn submit_capstone(ws: &Path, slug: &str, content: &str, today: Date) → AppResult<Manifest>
pub fn submit_placement_answer(ws: &Path, slug: &str, id: &str, answer: &str) → AppResult<()>
```

## site

### site/index.html
```
title: Lyceum — the evidence-based learning system, becoming an app
main#main
span#top
linearGradient#auGold
path#auArc
section#what
section#loop
section#architecture
section#today
```

### site/install.sh
```
# Lyceum — one-line installer for macOS.
function cleanup()
```
